#!/usr/bin/env bash
##PBS -N diskvert
##PBS -q short
##PBS -l walltime=1:0:0,mem=512MB
##PBS -o out 
##PBS -e err 

. /work/psk/gronki/environment.sh

cd $PBS_O_WORKDIR
bash job.sh $(head -n $PBS_ARRAYID jobs.txt | tail -1)

